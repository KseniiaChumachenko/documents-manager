{
  "extends": ["../../.eslintrc.json"],
  "ignorePatterns": ["!**/*", "dist/", "build/", ".react-router/types/**", "node_modules/**"],
  "overrides": [
    {
      "files": ["*.ts", "*.tsx", "*.js", "*.jsx"],
      "rules": {
        // Web app specific rules can be added here
      }
    }
  ]
}
