CREATE TABLE `my_company` (
	`id` integer PRIMARY KEY AUTOINCREMENT NOT NULL,
	`name` text NOT NULL,
	`egrpou` text,
	`inn` text,
	`vat_certificate` text,
	`iban` text,
	`bank_name` text,
	`mfo` text,
	`phone` text,
	`address` text,
	`tax_note` text,
	`signatory_name` text
);
